language: generic
sudo: required
services:
  - docker
  
before_install:
  - docker build -t efgstone/docker-react -f Dockerfile.dev .
  
script:
  - docker run -e CI=true efgstone/docker-react npm run test